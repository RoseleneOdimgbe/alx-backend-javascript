/**
 * Prints a message on the STDOUT (console)
 * @param message This is the message to print to the console
 */
function displayMessage(message) {
    console.log(message);
  }
  
  module.exports = displayMessage;
